/** @jsxRuntime classic **/
/** @jsx jsx */
import React from "react";
import { jsx } from "theme-ui";

type Props = {};

const Button: React.FC<Props> = ({}) => {
  return <button sx={{ variant: "buttons.simple" }} />;
};

export default Button;
